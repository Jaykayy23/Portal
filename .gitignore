node_modules/
.env
backend/.env
npm-debug.log*

# The live database — do NOT commit real delivery/account/password data.
# The repo ships a clean starter db.json; each install grows its own.
backend/data/db.json
!backend/data/.gitkeep
